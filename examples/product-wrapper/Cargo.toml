[package]
name = "osp-product-wrapper-example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", default-features = false, features = ["help", "std", "usage"] }
osp-cli = { path = "../.." }
