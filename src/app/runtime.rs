//! Runtime and launch-state types kept under the staged app layer.

pub use crate::osp_cli::state::{
    AppClients, AppRuntime, AppState, AuthState, ConfigState, LaunchContext, RuntimeContext,
    TerminalKind, UiState,
};
